include_rules

#include ../elfutils/use.tup

EXTRA_CFG_FLAGS = -DCMAKE_CXX_FLAGS='-DENABLE_VG_ANNOTATIONS'
#include ../../src/dyninst.tup

ifeq (y,@(LIMITED_MODE))
include ../../build/hasbuildtup.lua
ifeq (y,$(HAS_BUILD_TUP))
include build.tup
endif
endif
