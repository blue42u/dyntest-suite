ifeq ($(ELFUTILS_PREFIX),)
error ELFUTILS_PREFIX needs to be defined!
endif

ifeq ($(ELFUTILS_REAL_DIR),)
error Elfutils/use.tup should have been included before this!
endif

EX_DEPS += $(ELFUTILS_EX_DEP)
CFG_FLAGS += $(ELFUTILS_PREFIX)$(ELFUTILS_DUMMY_DIR)
TRANSFORMS += $(ELFUTILS_DUMMY_DIR)=$(ELFUTILS_REAL_DIR)
