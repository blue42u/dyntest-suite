include_rules

include findboost.lua
ifeq (y,$(BUILD_BOOST))
: |> ^o Built Boost^ ./boost.sh |> $(BOOST_FILES) <boost>
endif

include findtbb.lua
ifeq (y,$(BUILD_TBB))
: |> ^o Built TBB^ ./tbb.sh |> $(TBB_FILES) <tbb>
endif

include findxerces.lua
ifeq (y,$(BUILD_XERCES))
: |> ^o Built Xerces^ ./xerces.sh |> $(XERCES_FILES) <xerces>
endif
